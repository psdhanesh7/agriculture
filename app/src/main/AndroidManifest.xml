<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agriculture">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Cart"></activity>
        <activity android:name=".purchasePage" />
        <activity android:name=".CstmrDashBoard" />
        <activity android:name=".SoldDetailsActivity" />
        <activity android:name=".AddCorpsActivity" />
        <activity android:name=".FarmerPage" />
        <activity android:name=".CustomerLogin" />
        <activity android:name=".FarmerLogin" />
        <activity
            android:name=".ui.login.farmerLogin"
            android:label="@string/title_activity_farmer_login" />
        <activity android:name=".FarmerSignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>